import { AngularSettingEffect } from './angular-setting.effect';

describe('AngularSettingEffect', () => {
  it('should create an instance', () => {
    expect(new AngularSettingEffect()).toBeTruthy();
  });
});
