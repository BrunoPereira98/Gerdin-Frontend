import { AngularSettingSelector } from './angular-setting.selector';

describe('AngularSettingSelector', () => {
  it('should create an instance', () => {
    expect(new AngularSettingSelector()).toBeTruthy();
  });
});
