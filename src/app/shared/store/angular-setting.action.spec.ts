import { AngularSettingAction } from './angular-setting.action';

describe('AngularSettingAction', () => {
  it('should create an instance', () => {
    expect(new AngularSettingAction()).toBeTruthy();
  });
});
